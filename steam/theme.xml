<?xml version="1.0" encoding="UTF-8"?>
<theme>

    <variables>
        <themeColor>2ea8dd</themeColor>
        <selectColor>2ea8dd</selectColor>
    </variables>

    <include>./../theme.xml</include>

    <view name="system">
        <image name="background">
            <color>${themeColor}</color>
        </image>
        <image name="console_overlay">
            <path>./console.png</path>
        </image>
    </view>

    <variant name="textlistBasic">
        <view name="gamelist">
            <image name="background">
                <color>${themeColor}</color>
            </image>
            <textlist name="gamelist">
                <selectedColor>${selectColor}</selectedColor>
            </textlist>
        </view>
    </variant>

    <variant name="textlistWithImages">
        <view name="gamelist">
            <image name="background">
                <color>${themeColor}</color>
            </image>
            <textlist name="gamelist">
                <selectorColor>${themeColor}40</selectorColor>
            </textlist>
        </view>
    </variant>

    <variant name="textlistWithVideos">
        <view name="gamelist">
            <image name="background">
                <color>${themeColor}</color>
            </image>
            <textlist name="gamelist">
                <selectorColor>${themeColor}40</selectorColor>
            </textlist>
            <!-- Steam uses screenshots (its miximages are disabled — ES-DE's scraper
                 mislabels them), so re-add the game-logo marquee here. The main theme
                 drops md_marquee globally to avoid duplicating each miximage's logo. -->
            <!-- 2026-06-08 DISABLED: steam games now have miximages (logo BAKED IN), which the
                 user prefers, so md_marquee is dropped here (like the main theme) to remove the
                 double banner. Miximage games show one baked-in logo; screenshot-only steam games
                 show no overlay logo (same as every other system). Re-enable this block to restore.
            <image name="md_marquee">
                <pos>0.57 0.035</pos>
                <origin>0 0</origin>
                <maxSize>0.3 0.1</maxSize>
                <imageType>marquee</imageType>
                <zIndex>36</zIndex>
            </image> -->
        </view>
    </variant>

</theme>
